//const conn = require("../mysqldb");
//const router = require("express").Router();
//const bodyParser = require("body-parser");
//const conf = require("../configuration/config.json");
//const { RecyclerViewBackedScrollViewBase } = require("react-native");

//router.use(bodyParser.json());
//not sure whats needed not in love with this rn
//router.get('/get-data', function(req, res) {
  //  let sess = req.session;
   // console.log("request for login information");
  //
  //  if (sess) {
      //console.log(`Session: ${sess}`)
   //   console.log(JSON.stringify(sess));
   //   res.status(200).send('Hello ' + JSON.stringify(sess));
   // }
   // else {
   //   res.status(401).send("must login first");
  //  }
//  });

 // module.exports = router;